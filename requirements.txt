# MangaTrans — full manga translation pipeline dependencies
# Self-contained: cài file này (+ Python 3.10+) là đủ chạy MangaTrans/manga/.
#
# Yêu cầu hệ thống ngoài Python:
#   - Tesseract binary (cho pytesseract):
#     Windows: https://github.com/UB-Mannheim/tesseract/wiki
#     Linux:   apt install tesseract-ocr
#   - GPU driver phù hợp với bản torch/onnxruntime đã chọn.
#
# Asset model (giữ ở MangaTrans/ root):
#   - anime-manga-big-lama.pt        (196MB, LaMa inpainter, cần torch)
#   - comic-text-detector.onnx       (90MB, bubble detector, cần onnxruntime)
#   - Mali-Bold.ttf                  (font render text dịch)

# -------------------- Imaging --------------------
Pillow==12.2.0
opencv-python
numpy<2.0.0

# -------------------- HTTP (OpenRouter) --------------------
requests==2.34.2

# -------------------- PyTorch (cho LaMa inpainter) --------------------
# GPU CUDA 12.1:
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# -------------------- OCR engines --------------------
paddleocr==3.5.0
paddlepaddle==3.3.1
easyocr==1.7.2
manga-ocr==0.1.14
pytesseract==0.3.13


# -------------------- UI --------------------
gradio>=4.0.0
patool==2.2.0
